name: checking check-out

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: list files
        run: ls -la
      - name: view file readme.md
        run: cat README.md 
